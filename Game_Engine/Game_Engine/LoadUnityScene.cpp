#include "pch.h"
#include "LoadUnityScene.h"
#include "YAMLParser.h"

void LoadUnityScene::LoadScene(const std::string& path)
{
	/*int nodeSize = yaml->OpenFile(path)
	
	for (int i = 0; i < nodeSize; i++)
	{
		string name = yaml->FindNode(yaml->nodeList[i], m_Name?);
	}*/
}
